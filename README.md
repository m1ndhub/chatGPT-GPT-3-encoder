# GPT-3 Encoder

The string to be encoded and its token length calculated and show tokenID

# build image

```
docker build -t app .
```

# run container
```
docker-compose up -d
```

# down container 

```
docker-compose down
```
